# ASR(Plan 9)实现计划:麦克风 + 系统内录 流式转写

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 两路(mic 外录 + macOS 系统内录)流式 ASR:faster-whisper 在隔离子进程里转写,confirmed 段带词级时间戳进 `capture_events`,HUD 时间线实时呈现 confirmed/partial,原始音频存盘供回放/重转写。

**Architecture:** 纯逻辑(段落确认 `StreamState` + 幻觉过滤 `HallucinationFilter` + 引擎封装 `FasterWhisperEngine`)在后端可单测;ASR 编排跑独立 Python 子进程(`asr_worker`),mic 走 `sounddevice`、系统内录走移植的 Core Audio 原生 Swift helper(stdout 喂 PCM);子进程把 confirmed 段 POST 回后端落 `capture_events`、partial POST 到内存态经 SSE 推 HUD。模型 provision + 可调配置仿 MinerU/extraction。

**Tech Stack:** Python 3.11 / faster-whisper(CTranslate2)/ sounddevice / soundfile / numpy;Swift(系统音频 helper);FastAPI + SQLAlchemy;pytest(fakes);React + pywebview。

**约定(全程):**
- 后端测试 `cd backend && ./.venv/bin/pytest`(切勿裸 `python`);前端 `npm run build` 在 `frontend/`;Swift 语法/编译 `swiftc` 检查。
- git 身份已配好,**绝不** `git config`;commit trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- **绝不在入库内容/输出里写前身原型代号**;系统音频 helper 是从前身 `SystemAudioCapture.swift`(本地参考)移植,但代码注释/文件只写中性描述。
- 简体中文注释,代码/标识符英文(见 [[docs-in-simplified-chinese]])。
- 真 faster-whisper / sounddevice / Swift helper / 权限 = opt-in real-model 测 + 手测;纯逻辑全 TDD。
- 分支 `feat/plan-9-asr`。**不要 merge**——实现 + codex 审 + William 真机测后才合([[merge-only-after-manual-test]])。
- macOS 原生/子进程主线程坑见 [[pywebview-appkit-main-thread]];ASR 子进程隔离正为避开 [[macos-embedding-milvus-fork-order]]。

---

## 文件结构

**后端(新建)**
- `epictrace/asr/__init__.py`
- `epictrace/asr/types.py` — `WordTiming` / `TranscriptSegment`(重定义,带 source/words/confirmed)。
- `epictrace/asr/hallucination.py` — `HallucinationFilter`(§3 文本滤波)。
- `epictrace/asr/stream_state.py` — `StreamState`(每源段落确认 + never-drop + force-confirm)。
- `epictrace/asr/engine.py` — `FasterWhisperEngine`(实现 `Transcriber`,封装 faster-whisper;model 可注入)。
- `epictrace/asr/config.py` — `AsrConfig` dataclass + 默认值。
- `epictrace/asr/provisioner.py` — `AsrProvisioner`(faster-whisper 模型下载/状态,仿 MinerU)。
- `epictrace/asr/worker.py` — ASR 子进程入口(`python -m epictrace.asr.worker`):起音源 + 交替循环 + POST 回后端。
- `epictrace/asr/supervisor.py` — `AsrSupervisor`(后端侧:按 session 拉起/停止子进程)。
- `epictrace/asr/audio_sources.py` — `MicSource`(sounddevice)/ `SystemAudioSource`(Popen Swift helper);统一产 16kHz mono float32 帧。

**后端(修改)**
- `epictrace/interfaces/transcriber.py` — 重定义 `Transcriber` 为流式 + 新 `TranscriptSegment`。
- `epictrace/services/settings.py` — `get/set_asr_settings`。
- `epictrace/api/routers/capture.py` — start/stop/pause 联动 ASR 子进程;`POST /sessions/{id}/partial`;`/events/stream` 带 partial;`organize` 音频文件入库。
- `epictrace/api/routers/settings.py` — `GET/PUT /api/asr/settings`、`GET /api/asr/status`、`POST /api/asr/download-model`。
- `epictrace/config.py` — `asr_*` 默认(model 等);`pyproject.toml` 加依赖。

**原生(新建)**
- `shell/native/SystemAudioCapture.swift` — Core Audio process-tap → stdout 16kHz mono PCM(移植)。
- `shell/native/build.sh` — `swiftc` 构建到 `<data_dir>` 或仓库 bin。

**前端(修改)**
- `frontend/src/lib/api.ts` — asr 设置/状态;transcription 事件类型已被 CaptureEvent 覆盖(kind 字段)。
- `frontend/src/components/RecordingHud.tsx` — 启用 mic/system_audio 概念(源由采集视图选);时间线显示 transcription + partial(暂定态)。
- `frontend/src/views/CaptureView.tsx` — 启用 🎤外录 / 🔊内录 源开关。
- `frontend/src/views/SettingsView.tsx` — ASR 设置区(模型大小 + 下载 + 阈值)。

---

## Phase 1 — 纯逻辑核心(TDD,全 fake)

### Task 1: ASR 类型(重定义 TranscriptSegment）

**Files:**
- Modify: `backend/epictrace/interfaces/transcriber.py`
- Create: `backend/epictrace/asr/__init__.py`(空), `backend/epictrace/asr/types.py`
- Test: `backend/tests/test_asr_types.py`

- [ ] **Step 1: 写失败测试**
```python
# backend/tests/test_asr_types.py
from epictrace.asr.types import TranscriptSegment, WordTiming


def test_segment_holds_words_source_confirmed():
    seg = TranscriptSegment(
        text="你好世界", start=1.0, end=2.5, source="mic",
        words=[WordTiming(word="你好", start=1.0, end=1.6),
               WordTiming(word="世界", start=1.6, end=2.5)],
        confirmed=True,
    )
    assert seg.source == "mic" and seg.confirmed is True
    assert [w.word for w in seg.words] == ["你好", "世界"]
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_types.py -q`
Expected: FAIL（ModuleNotFoundError: epictrace.asr）

- [ ] **Step 3: 实现**
```python
# backend/epictrace/asr/__init__.py
# (空文件)
```
```python
# backend/epictrace/asr/types.py
from __future__ import annotations

from dataclasses import dataclass, field


@dataclass(frozen=True)
class WordTiming:
    word: str
    start: float  # 秒
    end: float


@dataclass(frozen=True)
class TranscriptSegment:
    text: str
    start: float
    end: float
    source: str  # "mic" | "device"
    words: list[WordTiming] = field(default_factory=list)
    confirmed: bool = False  # True=已确认(落库),False=partial(实时显示)
```
然后把 `interfaces/transcriber.py` 重定义为流式(替换旧的文件级 `transcribe`):
```python
# backend/epictrace/interfaces/transcriber.py
from __future__ import annotations

from typing import Protocol, runtime_checkable

from epictrace.asr.types import TranscriptSegment


@runtime_checkable
class Transcriber(Protocol):
    """流式 ASR 引擎封装:对一个滚动窗口的 PCM 做一次转写。
    流式循环(StreamState/交替/确认)在 asr.worker,不在这里。"""

    def transcribe_window(
        self, pcm, *, clip_start: float, prefix: str, source: str, language: str = "zh"
    ) -> list[TranscriptSegment]: ...


class NoopTranscriber:
    """占位:不转写(Plan 8 留的默认,保留给无引擎场景)。"""

    def transcribe_window(self, pcm, *, clip_start, prefix, source, language="zh"):
        return []
```
注:旧 `interfaces/transcriber.py` 里的 `TranscriptSegment`/`NoopTranscriber.transcribe` 被取代;全仓搜索 `from epictrace.interfaces.transcriber import` 确认只有 Plan 8 的占位测试引用(若 `tests/test_capture_interfaces.py` 测了旧 `transcribe` 空返回,改成测 `transcribe_window` 空返回)。

- [ ] **Step 4: 跑测试确认通过 + 不破旧测**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_types.py tests/test_capture_interfaces.py -q`
Expected: PASS（如 `test_capture_interfaces.py` 引用旧签名,同步改它)

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/asr/__init__.py backend/epictrace/asr/types.py backend/epictrace/interfaces/transcriber.py backend/tests/test_asr_types.py backend/tests/test_capture_interfaces.py
git commit -m "feat(asr): streaming TranscriptSegment + Transcriber.transcribe_window seam"
```

### Task 2: HallucinationFilter（§3 文本滤波）

**Files:**
- Create: `backend/epictrace/asr/hallucination.py`
- Test: `backend/tests/test_asr_hallucination.py`

- [ ] **Step 1: 写失败测试**
```python
# backend/tests/test_asr_hallucination.py
from epictrace.asr.hallucination import HallucinationFilter


def test_drops_chinese_near_silence_hallucinations():
    f = HallucinationFilter()
    assert f.is_hallucination("谢谢观看")
    assert f.is_hallucination("请不吝点赞 订阅 转发")  # 子串命中
    assert not f.is_hallucination("虚拟内存的工作原理是")


def test_drops_english_near_silence():
    f = HallucinationFilter()
    assert f.is_hallucination("Thank you for watching")
    assert f.is_hallucination("you")
    assert not f.is_hallucination("the cache line is 64 bytes")


def test_recent_duplicate_detection():
    f = HallucinationFilter()
    recent = ["这是一段测试", "这是一段测试 内容"]
    assert f.is_duplicate("这是一段测试", recent)
    assert not f.is_duplicate("完全不同的句子", recent)


def test_repetition_loop_signature():
    f = HallucinationFilter()
    sig1 = f.signature("好 的 好 的")
    sig2 = f.signature("好的好的")
    # 归一化为词 token 序列后比对(中文按字/词)
    assert isinstance(sig1, str) and sig1 == f.signature("好 的  好 的")
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_hallucination.py -q`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**
```python
# backend/epictrace/asr/hallucination.py
from __future__ import annotations

import re

# 近静音/标注幻觉精确串(中英),小写比对;移植自 ASR 笔记 §3.1
_EXACT = {
    "谢谢观看", "谢谢大家", "谢谢收看", "感谢观看", "感谢收看", "感谢大家",
    "thank you for watching", "thanks for watching", "bye", "goodbye",
    "you", "okay", "ok", "hmm", "um", "uh", "yeah", "right",
    "[silence]", "(silence)", "[blank_audio]", "[no speech]", "[inaudible]", "(inaudible)",
}
# 出现在更长文本里的中文幻觉子串(§3.2)
_SUBSTRINGS = (
    "请不吝点赞", "点赞订阅", "订阅转发", "打赏支持", "请订阅", "请点赞", "请转发",
    "字幕由", "字幕提供", "明镜与点点栏目",
)
_LEADING_PUNCT = re.compile(r"^[\W_]+", re.UNICODE)


class HallucinationFilter:
    """引擎无关的文本层幻觉过滤(§3)。阈值不当过滤器——这里才是真过滤。可配置开关/扩展。"""

    def __init__(self, *, enabled: bool = True, extra_exact: set[str] | None = None) -> None:
        self._enabled = enabled
        self._exact = {s.lower() for s in _EXACT} | {s.lower() for s in (extra_exact or set())}

    def clean(self, text: str) -> str:
        return _LEADING_PUNCT.sub("", text).strip()

    def is_hallucination(self, text: str) -> bool:
        if not self._enabled:
            return False
        t = self.clean(text).lower()
        if not t:
            return True
        if t in self._exact:
            return True
        if t.startswith("(speaking") and t.endswith(")"):
            return True
        for sub in _SUBSTRINGS:
            if sub in text:
                return True
        return False

    def signature(self, text: str) -> str:
        # 归一化为去空白/标点的字符序列,供「连续 N 次相同 hypothesis」比对
        return re.sub(r"\s+", "", self.clean(text)).lower()

    def is_duplicate(self, text: str, recent: list[str]) -> bool:
        sig = self.signature(text)
        if not sig:
            return True
        for r in recent:
            rsig = self.signature(r)
            if sig == rsig or (len(sig) >= 4 and (sig in rsig or rsig in sig)):
                return True
        return False
```

- [ ] **Step 4: 跑测试确认通过**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_hallucination.py -q`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/asr/hallucination.py backend/tests/test_asr_hallucination.py
git commit -m "feat(asr): HallucinationFilter (silence/near-silence patterns + dedup + signature)"
```

### Task 3: StreamState（段落确认 + never-drop + force-confirm）

**Files:**
- Create: `backend/epictrace/asr/stream_state.py`
- Test: `backend/tests/test_asr_stream_state.py`

- [ ] **Step 1: 写失败测试**
```python
# backend/tests/test_asr_stream_state.py
from epictrace.asr.hallucination import HallucinationFilter
from epictrace.asr.stream_state import StreamState
from epictrace.asr.types import TranscriptSegment


def _seg(text, s, e):
    return TranscriptSegment(text=text, start=s, end=e, source="mic", words=[], confirmed=False)


def test_confirms_all_but_last_segment():
    st = StreamState(source="mic", filter=HallucinationFilter(), force_confirm_after=4)
    confirmed = st.ingest([_seg("第一句", 0, 2), _seg("第二句", 2, 4), _seg("第三句", 4, 6)])
    # 除最后一段外确认;partial=最后一段
    assert [c.text for c in confirmed] == ["第一句", "第二句"]
    assert st.partial.text == "第三句"
    assert st.last_confirmed_end == 4  # 推到第二段末


def test_hallucination_segment_dropped_but_not_seek_loss():
    st = StreamState(source="mic", filter=HallucinationFilter(), force_confirm_after=4)
    confirmed = st.ingest([_seg("谢谢观看", 0, 2), _seg("真实内容", 2, 4)])
    # 幻觉段被滤掉、不进 confirmed;但 last_confirmed_end 仍按真实进展推进(不丢音)
    assert all("谢谢观看" not in c.text for c in confirmed)


def test_force_confirm_after_no_progress():
    st = StreamState(source="mic", filter=HallucinationFilter(), force_confirm_after=2)
    # 单段反复(弱音不自然确认):连续无进展 N 轮后强制确认该段
    for _ in range(2):
        st.ingest([_seg("勉强这一句", 0, 2)])
    confirmed = st.ingest([_seg("勉强这一句", 0, 2)])
    assert any("勉强这一句" in c.text for c in confirmed)


def test_duplicate_confirmed_suppressed():
    st = StreamState(source="mic", filter=HallucinationFilter(), force_confirm_after=4)
    st.ingest([_seg("重复句", 0, 2), _seg("x", 2, 3)])
    again = st.ingest([_seg("重复句", 0, 2), _seg("y", 2, 3)])
    assert all("重复句" not in c.text for c in again)  # 最近 N 去重
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_stream_state.py -q`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**
```python
# backend/epictrace/asr/stream_state.py
from __future__ import annotations

from collections import deque

from epictrace.asr.hallucination import HallucinationFilter
from epictrace.asr.types import TranscriptSegment


class StreamState:
    """单源(mic/device)流式确认。确认纪律(ASR 笔记 §3.6/§4):
    - 一窗多段 → 除最后一段外确认并 emit,最后一段作 partial;
    - 幻觉段(HallucinationFilter)/最近 N 重复 → 不 emit;
    - **绝不拿阈值丢段**;无进展 force_confirm_after 轮 → 强制确认当前 partial(防弱音卡死)。"""

    def __init__(self, source: str, filter: HallucinationFilter, *,
                 force_confirm_after: int = 4, recent_max: int = 5) -> None:
        self._source = source
        self._filter = filter
        self._force_after = force_confirm_after
        self.last_confirmed_end: float = 0.0
        self.partial: TranscriptSegment | None = None
        self._recent: deque[str] = deque(maxlen=recent_max)
        self._rounds_no_progress = 0

    def _accept(self, seg: TranscriptSegment) -> bool:
        if self._filter.is_hallucination(seg.text):
            return False
        if self._filter.is_duplicate(seg.text, list(self._recent)):
            return False
        return True

    def _confirm(self, seg: TranscriptSegment, out: list[TranscriptSegment]) -> None:
        if self._accept(seg):
            self._recent.append(seg.text)
            out.append(TranscriptSegment(
                text=self._filter.clean(seg.text), start=seg.start, end=seg.end,
                source=self._source, words=seg.words, confirmed=True))
        self.last_confirmed_end = max(self.last_confirmed_end, seg.end)

    def ingest(self, segments: list[TranscriptSegment]) -> list[TranscriptSegment]:
        out: list[TranscriptSegment] = []
        if not segments:
            return out
        if len(segments) > 1:
            for seg in segments[:-1]:
                self._confirm(seg, out)
            self.partial = segments[-1]
            self._rounds_no_progress = 0
        else:
            # 只有一段:作 partial;若连续 N 轮没新确认 → 强制确认它(弱音防卡死)
            self.partial = segments[0]
            self._rounds_no_progress += 1
            if self._rounds_no_progress >= self._force_after:
                self._confirm(segments[0], out)
                self.partial = None
                self._rounds_no_progress = 0
        if out:
            self._rounds_no_progress = 0
        return out
```

- [ ] **Step 4: 跑测试确认通过**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_stream_state.py -q`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/asr/stream_state.py backend/tests/test_asr_stream_state.py
git commit -m "feat(asr): StreamState segment confirmation (never-drop + force-confirm + dedup)"
```

### Task 4: AsrConfig（可调配置默认）

**Files:**
- Create: `backend/epictrace/asr/config.py`
- Test: `backend/tests/test_asr_config.py`

- [ ] **Step 1: 写失败测试**
```python
# backend/tests/test_asr_config.py
from epictrace.asr.config import AsrConfig


def test_defaults_are_weak_audio_friendly():
    c = AsrConfig()
    assert c.model == "large-v3"
    assert c.language == "zh"
    assert c.vad is True
    assert c.condition_prev is False        # 防幻觉沿前文传染
    assert c.force_confirm_after == 4
    assert c.no_speech == 0.6 and c.log_prob == -1.0 and c.compression_ratio == 2.4


def test_from_dict_overrides():
    c = AsrConfig.from_dict({"model": "medium", "vad": False})
    assert c.model == "medium" and c.vad is False and c.language == "zh"
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_config.py -q`
Expected: FAIL

- [ ] **Step 3: 实现**
```python
# backend/epictrace/asr/config.py
from __future__ import annotations

from dataclasses import asdict, dataclass


@dataclass(frozen=True)
class AsrConfig:
    model: str = "large-v3"            # large-v3 / distil-large-v3 / medium / small
    language: str = "zh"
    vad: bool = True
    vad_threshold: float = 0.5
    no_speech: float = 0.6
    log_prob: float = -1.0
    compression_ratio: float = 2.4
    repetition_penalty: float = 1.0
    no_repeat_ngram: int = 0
    condition_prev: bool = False
    halluc_silence: float | None = 2.0
    force_confirm_after: int = 4
    stall_seek_seconds: float = 0.8
    rms_normalize: bool = True
    halluc_filter_enabled: bool = True

    _VALID_MODELS = ("large-v3", "distil-large-v3", "medium", "small")

    @classmethod
    def from_dict(cls, d: dict) -> "AsrConfig":
        base = asdict(cls())
        for k, v in (d or {}).items():
            if k in base:
                base[k] = v
        base.pop("_VALID_MODELS", None)
        return cls(**base)

    def to_dict(self) -> dict:
        return asdict(self)
```

- [ ] **Step 4: 跑测试确认通过**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_config.py -q`
Expected: PASS

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/asr/config.py backend/tests/test_asr_config.py
git commit -m "feat(asr): AsrConfig with weak-audio-friendly defaults"
```

---

## Phase 2 — 引擎封装 + 交替循环(TDD with fakes)

### Task 5: FasterWhisperEngine（封装,model 可注入）

**Files:**
- Create: `backend/epictrace/asr/engine.py`
- Test: `backend/tests/test_asr_engine.py`

- [ ] **Step 1: 写失败测试**(注入假 faster-whisper model,验证参数透传 + segment 映射)
```python
# backend/tests/test_asr_engine.py
from epictrace.asr.config import AsrConfig
from epictrace.asr.engine import FasterWhisperEngine


class _FakeWord:
    def __init__(self, w, s, e): self.word, self.start, self.end = w, s, e


class _FakeSeg:
    def __init__(self, text, s, e, words): self.text, self.start, self.end, self.words = text, s, e, words


class _FakeModel:
    def __init__(self): self.calls = []
    def transcribe(self, pcm, **opts):
        self.calls.append(opts)
        segs = [_FakeSeg(" 你好", 0.0, 1.0, [_FakeWord("你好", 0.0, 1.0)])]
        info = type("I", (), {"language": "zh"})()
        return iter(segs), info


def test_engine_maps_segments_and_passes_options():
    model = _FakeModel()
    eng = FasterWhisperEngine(model, AsrConfig())
    out = eng.transcribe_window(b"pcm", clip_start=3.0, prefix="上一句", source="mic")
    assert out[0].text == " 你好" and out[0].source == "mic"
    assert out[0].words[0].word == "你好"
    opts = model.calls[0]
    assert opts["clip_timestamps"] == "3.0"
    assert opts["initial_prompt"] == "上一句"
    assert opts["word_timestamps"] is True
    assert opts["language"] == "zh"
    assert opts["condition_on_previous_text"] is False
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_engine.py -q`
Expected: FAIL

- [ ] **Step 3: 实现**
```python
# backend/epictrace/asr/engine.py
from __future__ import annotations

from epictrace.asr.config import AsrConfig
from epictrace.asr.types import TranscriptSegment, WordTiming


class FasterWhisperEngine:
    """封装 faster-whisper 的单窗口转写。model 注入(测试用假件;生产 = WhisperModel)。"""

    def __init__(self, model, config: AsrConfig) -> None:
        self._model = model
        self._cfg = config

    def transcribe_window(self, pcm, *, clip_start: float, prefix: str,
                          source: str, language: str | None = None) -> list[TranscriptSegment]:
        cfg = self._cfg
        segments, _info = self._model.transcribe(
            pcm,
            language=language or cfg.language,
            task="transcribe",
            clip_timestamps=f"{clip_start}",
            initial_prompt=prefix or None,
            word_timestamps=True,
            suppress_blank=True,
            temperature=[0.0, 0.2],
            compression_ratio_threshold=cfg.compression_ratio,
            log_prob_threshold=cfg.log_prob,
            no_speech_threshold=cfg.no_speech,
            vad_filter=cfg.vad,
            vad_parameters={"threshold": cfg.vad_threshold},
            hallucination_silence_threshold=cfg.halluc_silence,
            repetition_penalty=cfg.repetition_penalty,
            no_repeat_ngram_size=cfg.no_repeat_ngram,
            condition_on_previous_text=cfg.condition_prev,
        )
        out: list[TranscriptSegment] = []
        for s in segments:
            words = [WordTiming(word=w.word, start=w.start, end=w.end)
                     for w in (getattr(s, "words", None) or [])]
            out.append(TranscriptSegment(
                text=s.text, start=s.start, end=s.end, source=source,
                words=words, confirmed=False))
        return out
```

- [ ] **Step 4: 跑测试确认通过**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_engine.py -q`
Expected: PASS

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/asr/engine.py backend/tests/test_asr_engine.py
git commit -m "feat(asr): FasterWhisperEngine window wrapper (options per spec §7)"
```

### Task 6: 交替双流循环（StreamLoop,纯逻辑,假音源+假引擎）

**Files:**
- Create: `backend/epictrace/asr/loop.py`(`StreamLoop`:选源 + 推进 + 产出 confirmed/partial 回调)
- Test: `backend/tests/test_asr_loop.py`

- [ ] **Step 1: 写失败测试**
```python
# backend/tests/test_asr_loop.py
from epictrace.asr.config import AsrConfig
from epictrace.asr.loop import StreamLoop
from epictrace.asr.types import TranscriptSegment


class _FakeEngine:
    """按 (source, clip_start) 返回预设段。"""
    def __init__(self, script): self.script = script; self.seen = []
    def transcribe_window(self, pcm, *, clip_start, prefix, source, language=None):
        self.seen.append((source, clip_start))
        return self.script.get((source, round(clip_start, 1)), [])


def _seg(t, s, e, src):
    return TranscriptSegment(text=t, start=s, end=e, source=src, words=[], confirmed=False)


def test_alternates_to_source_with_more_pending_audio():
    eng = _FakeEngine({("device", 0.0): [_seg("讲座内容", 0, 2, "device"), _seg("x", 2, 3, "device")]})
    confirmed = []
    loop = StreamLoop(eng, AsrConfig(), on_confirmed=lambda s: confirmed.append(s), on_partial=lambda s: None)
    # device 路有 3s 待处理,mic 只 0.5s → 选 device
    loop.set_pending(mic=0.5, device=3.0)
    loop.tick(audio={"mic": b"", "device": b""})
    assert eng.seen[0][0] == "device"
    assert any(c.source == "device" and "讲座内容" in c.text for c in confirmed)
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_loop.py -q`
Expected: FAIL

- [ ] **Step 3: 实现**
```python
# backend/epictrace/asr/loop.py
from __future__ import annotations

from collections.abc import Callable

from epictrace.asr.config import AsrConfig
from epictrace.asr.hallucination import HallucinationFilter
from epictrace.asr.stream_state import StreamState
from epictrace.asr.types import TranscriptSegment

_MIN_PENDING = 1.0  # 秒:某路待处理音频超过它才值得转一轮


class StreamLoop:
    """单引擎在 mic/device 间逐轮交替(ASR 笔记 §5)。纯逻辑:音频与 pending 由调用方喂。"""

    def __init__(self, engine, config: AsrConfig,
                 on_confirmed: Callable[[TranscriptSegment], None],
                 on_partial: Callable[[TranscriptSegment], None]) -> None:
        self._engine = engine
        self._cfg = config
        self._on_confirmed = on_confirmed
        self._on_partial = on_partial
        f = HallucinationFilter(enabled=config.halluc_filter_enabled)
        self._states = {
            "mic": StreamState("mic", f, force_confirm_after=config.force_confirm_after),
            "device": StreamState("device", f, force_confirm_after=config.force_confirm_after),
        }
        self._pending = {"mic": 0.0, "device": 0.0}

    def set_pending(self, *, mic: float, device: float) -> None:
        self._pending = {"mic": mic, "device": device}

    def _pick_source(self) -> str | None:
        cand = [(p, s) for s, p in self._pending.items() if p >= _MIN_PENDING]
        if not cand:
            return None
        return max(cand)[1]

    def tick(self, audio: dict) -> None:
        src = self._pick_source()
        if src is None:
            return
        st = self._states[src]
        prefix = st.partial.text if st.partial else ""
        segs = self._engine.transcribe_window(
            audio[src], clip_start=st.last_confirmed_end, prefix=prefix, source=src)
        for c in st.ingest(segs):
            self._on_confirmed(c)
        if st.partial is not None:
            self._on_partial(st.partial)
```

- [ ] **Step 4: 跑测试确认通过**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_loop.py -q`
Expected: PASS

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/asr/loop.py backend/tests/test_asr_loop.py
git commit -m "feat(asr): StreamLoop alternating dual-source driver (pure logic)"
```

---

## Phase 3 — 模型 provision + 设置(TDD)

### Task 7: AsrProvisioner（faster-whisper 模型下载/状态）

**Files:**
- Create: `backend/epictrace/asr/provisioner.py`
- Test: `backend/tests/test_asr_provisioner.py`

- [ ] **Step 1: 写失败测试**(仿 mineru_provisioner,注入假下载 runner + tmp 缓存)
```python
# backend/tests/test_asr_provisioner.py
from pathlib import Path

from epictrace.asr.provisioner import AsrProvisioner


def test_not_ready_when_model_absent(tmp_path: Path):
    prov = AsrProvisioner(cache_dir=tmp_path)
    assert prov.is_ready("large-v3") is False
    assert prov.state == "not_downloaded"


def test_download_then_ready(tmp_path: Path):
    calls = []
    def fake_runner(model, cache_dir, progress_cb):
        # 假装下载:在缓存里建出 faster-whisper 期望的目录
        (cache_dir / f"models--Systran--faster-whisper-{model}").mkdir(parents=True)
        progress_cb("done")
        calls.append(model)
    prov = AsrProvisioner(cache_dir=tmp_path, download_runner=fake_runner)
    prov.download_model("large-v3", progress_cb=lambda m: None)
    assert calls == ["large-v3"]
    assert prov.is_ready("large-v3") is True
    assert prov.state == "ready"
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_provisioner.py -q`
Expected: FAIL

- [ ] **Step 3: 实现**(`is_ready` = HF 缓存里存在 `models--Systran--faster-whisper-<model>` 或 `models--*distil*`;`download_model` 默认 runner 用 `huggingface_hub.snapshot_download` 或实例化 `WhisperModel` 触发下载;可注入。状态机 `not_downloaded → downloading → ready / failed` + `last_error`。)参照 `media/mineru_provisioner.py` 的锁 + 状态 + 注入风格,代码完整写出(`_default_runner` 用 `from faster_whisper import WhisperModel; WhisperModel(model, download_root=cache_dir)` 触发下载;`detect` 用 glob)。

- [ ] **Step 4: 跑测试确认通过**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_provisioner.py -q`
Expected: PASS

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/asr/provisioner.py backend/tests/test_asr_provisioner.py
git commit -m "feat(asr): AsrProvisioner — faster-whisper model download + readiness"
```

### Task 8: SettingsService.get/set_asr_settings

**Files:**
- Modify: `backend/epictrace/services/settings.py`
- Test: `backend/tests/test_asr_settings_service.py`

- [ ] **Step 1: 写失败测试**(仿 `test_api_settings`/extraction:默认 = AsrConfig 默认;校验 model∈_VALID_MODELS;PUT 部分键合并;`_load` 保留其它顶层键如 `extraction`/`profiles`)
```python
# backend/tests/test_asr_settings_service.py
from pathlib import Path

import pytest

from epictrace.config import AppConfig
from epictrace.services.settings import SettingsService


def test_default_asr_settings(tmp_path: Path):
    svc = SettingsService(AppConfig(data_dir=tmp_path))
    s = svc.get_asr_settings()
    assert s["model"] == "large-v3" and s["language"] == "zh" and s["vad"] is True


def test_set_and_validate(tmp_path: Path):
    svc = SettingsService(AppConfig(data_dir=tmp_path))
    svc.set_asr_settings({"model": "medium"})
    assert svc.get_asr_settings()["model"] == "medium"
    with pytest.raises(ValueError):
        svc.set_asr_settings({"model": "bogus"})


def test_set_asr_preserves_extraction(tmp_path: Path):
    svc = SettingsService(AppConfig(data_dir=tmp_path))
    svc.set_extraction_settings(engine="mineru", effort="high", model_source="modelscope")
    svc.set_asr_settings({"model": "small"})
    assert svc.get_extraction_settings()["engine"] == "mineru"  # 不被吞
```

- [ ] **Step 2: 跑测试确认失败**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_settings_service.py -q`
Expected: FAIL（AttributeError: get_asr_settings）

- [ ] **Step 3: 实现** — 在 `SettingsService` 加 `get_asr_settings()`(读 `asr` 顶层键,缺则 `AsrConfig().to_dict()`)/ `set_asr_settings(d)`(校验 model;合并进现有;走既有 `_read_raw`/`_write` + 锁 + 原子写,**保留非 asr 顶层键**)。参照现有 `get/set_extraction_settings` 的实现完整写出。

- [ ] **Step 4: 跑测试确认通过**
Run: `cd backend && ./.venv/bin/pytest tests/test_asr_settings_service.py -q`
Expected: PASS

- [ ] **Step 5: 提交**
```bash
git add backend/epictrace/services/settings.py backend/tests/test_asr_settings_service.py
git commit -m "feat(asr): persist ASR settings (get/set, validated, preserves other keys)"
```

---

## Phase 4 — 后端接线:子进程编排 + 事件 + partial + API(TDD where可测)

### Task 9: 依赖 + AppConfig 字段

**Files:**
- Modify: `backend/pyproject.toml`(加 `faster-whisper`, `sounddevice`, `soundfile`), `backend/epictrace/config.py`(`asr_cache_dir` 属性 = `data_dir/".asr-models"` 或 HF 缓存)
- Test: `backend/tests/test_asr_deps.py`(`import faster_whisper` / `import sounddevice` 可用——**opt-in `@pytest.mark.skipif` 没装则跳过**,因 CI 不一定装重依赖)

- [ ] **Step 1-5**: 加依赖到 pyproject;`.venv/bin/pip install -e .`(或 `pip install faster-whisper sounddevice soundfile`);写一个 import smoke 测(装了才跑);config 加 `asr_model_dir` 属性。提交 `chore(asr): add faster-whisper/sounddevice deps + asr cache dir`。
> 注:`faster-whisper` 拉 CTranslate2(大);`sounddevice` 需系统 PortAudio(`brew install portaudio` 或 wheel 自带)。装失败则记录,真机环境手装。

### Task 10: 子进程编排 worker + supervisor + 音源

**Files:**
- Create: `backend/epictrace/asr/audio_sources.py`, `backend/epictrace/asr/worker.py`, `backend/epictrace/asr/supervisor.py`
- Test: `backend/tests/test_asr_supervisor.py`(假 Popen,验证按 sources 拉起/停止 + 命令行参数)

- [ ] **Step 1: 写失败测试**(supervisor 注入假 spawn)
```python
# backend/tests/test_asr_supervisor.py
from epictrace.asr.supervisor import AsrSupervisor


def test_starts_worker_only_when_audio_source_selected():
    spawned = []
    sup = AsrSupervisor(spawn=lambda argv: spawned.append(argv) or object())
    sup.start(session_id=1, sources=["note", "clipboard"], staging_dir="/tmp/1")
    assert spawned == []                      # 无音频源 → 不起
    sup.start(session_id=2, sources=["mic"], staging_dir="/tmp/2")
    assert len(spawned) == 1 and "--session" in spawned[0]


def test_stop_terminates():
    procs = []
    class _P:
        def __init__(self): self.killed = False
        def terminate(self): self.killed = True
        def poll(self): return None
    sup = AsrSupervisor(spawn=lambda argv: procs.append(_P()) or procs[-1])
    sup.start(session_id=3, sources=["system_audio"], staging_dir="/tmp/3")
    sup.stop(3)
    assert procs[0].killed is True
```

- [ ] **Step 2: 跑确认失败** → **Step 3: 实现**:
  - `AsrSupervisor`：`start(session_id, sources, staging_dir)` 仅当 `"mic"`/`"system_audio"` ∈ sources 才 `spawn(["python","-m","epictrace.asr.worker","--session",sid,"--sources",...,"--staging",dir,"--model",model])`,存进 `{sid: proc}`;`stop(sid)` terminate;`spawn` 可注入(测试)。
  - `worker.py`：解析 argv → 起选中音源(`MicSource`/`SystemAudioSource`)→ 建 `FasterWhisperEngine`(provision 已就绪的模型)+ `StreamLoop` → 循环 `tick`,`on_confirmed`=POST `/sessions/{sid}/events`(kind=transcription),`on_partial`=POST `/sessions/{sid}/partial`;音源同时把 PCM 落 `staging/audio-{source}.wav`(soundfile 追加)。**worker 本身不单测**(集成);仅 `argv 解析`可加小测。
  - `audio_sources.py`：`MicSource`(sounddevice InputStream 16k mono,环形 buffer,watchdog)/ `SystemAudioSource`(Popen 原生 helper,读 stdout PCM);统一接口 `read() -> np.ndarray` + `pending_seconds()`。**真采集手测**;`pending`/环形 buffer 的纯逻辑可小测。
- [ ] **Step 4-5**: 跑 supervisor 测 PASS;`python -c "import ast; ast.parse(open('epictrace/asr/worker.py').read())"` 语法自检;提交 `feat(asr): worker subprocess + supervisor + audio sources`。

### Task 11: 采集路由联动 ASR + transcription 事件 + partial + SSE

**Files:**
- Modify: `backend/epictrace/api/routers/capture.py`, `backend/epictrace/api/app.py`(`app.state.asr_supervisor` + `app.state.asr_partials = {}`)
- Test: `backend/tests/test_api_asr_capture.py`

- [ ] **Step 1: 写失败测试**(注入假 supervisor;验证 start 选音频源→supervisor.start 被调;POST partial→GET 能读;transcription 事件经 events 落库)
```python
# backend/tests/test_api_asr_capture.py
from pathlib import Path
from fastapi.testclient import TestClient
from epictrace.api.app import create_app
from epictrace.config import AppConfig
from epictrace.db import Database


def _client(tmp_path, sup):
    db = Database(AppConfig(data_dir=tmp_path)); db.create_all()
    app = create_app(db=db)
    app.state.asr_supervisor = sup
    return TestClient(app)


def test_start_with_mic_triggers_supervisor(tmp_path):
    started = []
    class _Sup:
        def start(self, **kw): started.append(kw)
        def stop(self, sid): pass
    c = _client(tmp_path, _Sup())
    sid = c.post("/api/capture/sessions", json={"sources": ["mic", "note"]}).json()["id"]
    assert started and started[0]["session_id"] == sid

def test_partial_roundtrip(tmp_path):
    class _Sup:
        def start(self, **kw): pass
        def stop(self, sid): pass
    c = _client(tmp_path, _Sup())
    sid = c.post("/api/capture/sessions", json={"sources": ["mic"]}).json()["id"]
    c.post(f"/api/capture/sessions/{sid}/partial", json={"source": "mic", "text": "暂定文本"})
    # transcription 段落经普通事件入库
    c.post(f"/api/capture/sessions/{sid}/events",
           json={"kind": "transcription", "payload": "确认文本", "meta": {"source": "mic"}})
    detail = c.get(f"/api/capture/sessions/{sid}").json()
    assert any(e["kind"] == "transcription" for e in detail["events"])
```

- [ ] **Step 2: 跑确认失败** → **Step 3: 实现**:
  - `app.py`:`app.state.asr_supervisor`(默认 `AsrSupervisor()` 真件,可注入)+ `app.state.asr_partials = {}`。
  - `start_session`:CaptureService.start 后,若 sources∩{mic,system_audio} 非空 → `asr_supervisor.start(session_id=…, sources=…, staging_dir=…)`(catch 异常 → 日志,不挡 session)。
  - `stop_session` / `delete`:`asr_supervisor.stop(sid)`。
  - pause/resume:`asr_supervisor.pause(sid)/resume(sid)`(stop/restart worker 或发信号;最简先 stop+start)。
  - 新 `POST /sessions/{sid}/partial {source, text}` → 存 `app.state.asr_partials[sid][source]=text`(内存)。
  - `/events/stream` 生成器每轮**额外 yield partial 快照**(`event: partial`)。
  - `organize`:OrganizeService 已把 staging 文件入库——音频 wav 作为文件进 Project(Plan 8 的物化逻辑加上把 `audio-*.wav` 也 ingest;若 OrganizeService 只搬 markdown+screenshot,扩展它把音频文件也纳入——**改 `OrganizeService` + 加测**)。
- [ ] **Step 4-6**: 跑测 PASS;**全后端套件** `./.venv/bin/pytest -q` 不回归;提交 `feat(asr): wire ASR subprocess into capture lifecycle + partial SSE + transcription events`。

---

## Phase 5 — 系统内录原生 helper(手测,Swift)

### Task 12: SystemAudioCapture Swift helper（移植 + 构建)

**Files:**
- Create: `shell/native/SystemAudioCapture.swift`, `shell/native/build.sh`

- [ ] **Step 1: 移植**:从前身本地参考实现移植 Core Audio process-tap → 聚合设备 → 读**聚合设备真实采样率** → `AVAudioConverter` 重采样到 16kHz mono Float32 → 裸 PCM(float32 le)写 `stdout`。要点(ASR 笔记 §5):`CATapDescription` 全局 stereo、排除自身 pid、`.unmuted`、`.isPrivate`;`kAudioAggregateDeviceTapAutoStartKey=false`;清理顺序;权限启发式(~10s 近零能量 → stderr 报 `PERMISSION_DENIED`)。**代码注释中性,不写任何前身代号。**
- [ ] **Step 2: 构建脚本**:`build.sh` = `swiftc -O SystemAudioCapture.swift -o "$1/epictrace-sysaudio"`(输出到 data_dir/bin)。`SystemAudioSource` 用该二进制路径 Popen。
- [ ] **Step 3: 编译自检**:`swiftc -typecheck shell/native/SystemAudioCapture.swift`(或在 macOS 上 `swiftc -O` 出二进制)→ 无错。**真采集 = 真机手测**(播放音频 → 看 transcription 出现;含采样率/权限验证)。
- [ ] **Step 4: 提交** `feat(asr): macOS system-audio capture helper (Core Audio process tap)`。

---

## Phase 6 — 前端(门禁 build,行为手测)

### Task 13: ASR 设置 client + 设置区

**Files:** `frontend/src/lib/api.ts`(`getAsrSettings`/`putAsrSettings`/`getAsrStatus`/`downloadAsrModel`)、`frontend/src/views/SettingsView.tsx`(ASR 区:模型大小下拉 + 下载/进度 + VAD/阈值高级项折叠)。
- [ ] 实现 → `npm run build` exit 0 → 提交 `feat(asr): ASR settings UI + client`。

### Task 14: 启用源开关 + HUD/时间线显示 transcription + partial

**Files:** `frontend/src/views/CaptureView.tsx`(🎤外录/🔊内录 `disabled:false`,选中即纳入 startSession 的 sources)、`frontend/src/components/RecordingHud.tsx`(时间线已能显示任意 kind;为 `transcription` 加来源色点 + 文本;消费 `/events/stream` 的 `partial` 事件 → 末尾淡显「暂定」行)。
- [ ] 实现 → `npm run build` exit 0 → 提交 `feat(asr): enable mic/system-audio sources + live transcription/partial in HUD`。
> 说明:`CaptureEvent` 已是通用 `{kind,payload,meta}`,transcription 不需新类型;partial 走 SSE `event: partial`,前端单独 state 渲染、不混进 events 列表。

---

## Phase 7 — 评测 + 收尾

### Task 15: 离线评测脚本 + 收尾

**Files:** `backend/scripts/asr_eval.py`(喂样例 wav → 跑 `FasterWhisperEngine`+`StreamLoop` → 打印 transcript + 命中的过滤;**不进 CI**,手动 + 接 Langfuse 计划)。
- [ ] **Step 1**: 写脚本(读 wav → 16k mono → 模拟流式喂入 → 输出)。
- [ ] **Step 2**: 全后端套件 `./.venv/bin/pytest -q` 绿 + 前端 `npm run build` exit 0 + Swift `swiftc -typecheck` 无错。
- [ ] **Step 3**: 提交 `chore(asr): offline ASR eval script + finalize`。

---

## 自审(对照 spec)

- **§2 两路音源**:Task 10(MicSource/SystemAudioSource)+ Task 12(Swift helper)✓。
- **§3 进程模型**:Task 10(worker/supervisor 子进程)+ Task 11(后端联动 + POST 回)✓。
- **§4 交替/弱音**:Task 6(StreamLoop 选源)+ §5/Task10(RMS 归一化在音源)——**注:RMS 归一化放 `audio_sources.py`,Task 10 实现时带上**(自审补:Task 10 step3 显式加 `rms_normalize`)。
- **§5 转写三块**:Task 5(Engine)+ Task 3(StreamState)+ Task 2(HallucinationFilter)✓。
- **§6 事件 + 音频**:Task 1(TranscriptSegment)+ Task 11(transcription 事件 + organize 音频入库)+ Task 10(音源落 wav)✓。
- **§7 参数映射**:Task 5 的 `transcribe` opts 逐条对上 ✓。
- **§8 provision/依赖**:Task 7 + Task 9 ✓。
- **§9 配置 + 评测**:Task 4 + Task 8(持久化)+ Task 15(评测)✓。
- **§10 HUD/UI**:Task 14 ✓。
- **§11 权限/降级**:Task 10(mic watchdog)+ Task 12(系统音频权限启发式 + 采样率)+ Task 11(子进程失败降级)✓。
- **§12 错误边界**:Task 11(start 失败不挡 session、stop flush)+ Task 12(-10877 非致命)✓。
- **§13 测试**:Phase 1-4 全 fake TDD;真件/原生 opt-in + 手测 ✓。
- **占位扫描**:Task 7/8/10/12 引用「参照现有实现完整写出」——执行时按所指文件(mineru_provisioner / get_extraction_settings)落具体代码,非占位;StreamLoop 的 RMS 归一化已在自审补到 Task 10。
